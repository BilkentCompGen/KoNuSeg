
#pragma once

#include <data_structs.hpp>

void checkRepeats( SEQLIST *assembly, CBF *cbf, uint kMerSize, string seed1, long double genomeSize, uint windowSize, string outfile_bed, uint seg_len, uint count_threshold);
